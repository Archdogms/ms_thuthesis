# 论文逐章内容核查记录

检查时间：2026-05-31 14:09

## 一、已完成的重点核查

本轮按摘要、第 1 章至第 7 章、附录逐章检查了明显错误与前后口径冲突，重点关注数字口径、表题、公式、图表叙述、术语一致性和可能影响查重识别的 PDF 文本抽取。

## 二、已修正的主要问题

1. 统一非遗口径：正文按 90 项非遗统计，其中 36 项进入核心样本、54 项作为软性补充，避免原文中“55 项”的混用。
2. 将表 3.3 改为“165 条核心文化载体按类型分布”，避免把包含非遗空间锚点的样本误称为“物质文化载体”。
3. 修正 CMI 频次分值公式为 `log(1 + x)` 口径，与脚本计算方式一致，避免 `log(0)` 风险。
4. 修正 THI 公式说明：POI 数量和评论总量均按 `log(1 + x)` 后归一化，与脚本保持一致。
5. 补充 OAI = 25 的 15 条样本，完善官方认证指数分布说明。
6. 替换第 6 章表 6.1 的旧数据，更新为当前 `indices_town_summary.csv` 口径，并同步调整镇街解释。
7. 将镇街相关矩阵中的“非遗数”统一为“非遗项目数”，并说明七个镇街合计为 90 项。
8. 修正“南海农谣”为“南海农谚”，并调整空心景点区解释中与 CMI=0 不一致的表述。
9. 调整附录中“附录 C 列出完整清单”的矛盾表述，改为完整载体级指数表以电子附表留存。
10. 修正文中若干明显不规范表达，包括 `pdf文档`、英文分号、模型名称写法、`附录表单`、以及过强的“证明了”表述。

## 三、PDF 与查重识别复核

- `latexmk -xelatex` 已编译通过，生成 `ms-thesis.pdf`。
- `pdftotext -enc UTF-8 -nopgbrk` 可正常抽取中文正文。
- `pdffonts` 显示主要中英文字体均为嵌入、子集化且带 Unicode 映射。
- 抽取文本未发现 `(cid:)` 或替换字符 `�`。
- 目录和附表清单中未再出现“主桥梁清单”。

## 四、仍建议提交前人工看一眼

1. 封面姓名、学号、导师、日期是否与学院最终要求完全一致。
2. 参考文献的 DOI、年份、页码和中文译名是否需要按导师意见再精修。
3. 学院是否要求提交签字版声明、综合论文训练记录表或其他归档材料；这些通常不建议并入查重 PDF。
4. 地图和图片本身的视觉标注建议人工快速翻页确认，当前检查主要覆盖文本、表格和 PDF 可识别性。
